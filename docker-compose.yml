version: "3.8"

services:
  # FastAPI application (connects to remote PostgreSQL + Redis)
  api:
    build: .
    container_name: rag_api
    environment:
      DATABASE_URL: ${DATABASE_URL}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GEMINI_MODEL_NAME: ${GEMINI_MODEL_NAME}
      EMBEDDING_MODEL_NAME: ${EMBEDDING_MODEL_NAME}
      DIMENSION_OF_MODEL: ${DIMENSION_OF_MODEL}
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      REDIS_DB: ${REDIS_DB}
      WEBAPP_STORAGE_HOME: ${WEBAPP_STORAGE_HOME:-./site}
    ports:
      - "8000:8000"
    volumes:
      # Shared storage for file exchange between API and Worker
      - ./site/temp_data:/app/site/temp_data
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
    networks:
      - rag_network

  # Background Worker for document processing
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: rag_worker
    environment:
      DATABASE_URL: ${DATABASE_URL}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      GEMINI_MODEL_NAME: ${GEMINI_MODEL_NAME}
      EMBEDDING_MODEL_NAME: ${EMBEDDING_MODEL_NAME}
      DIMENSION_OF_MODEL: ${DIMENSION_OF_MODEL}
      CLOUDINARY_CLOUD_NAME: ${CLOUDINARY_CLOUD_NAME}
      CLOUDINARY_API_KEY: ${CLOUDINARY_API_KEY}
      CLOUDINARY_API_SECRET: ${CLOUDINARY_API_SECRET}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT}
      REDIS_DB: ${REDIS_DB}
      WEBAPP_STORAGE_HOME: ${WEBAPP_STORAGE_HOME:-./site}
    volumes:
      # Shared storage for file exchange between API and Worker
      - ./site/temp_data:/app/site/temp_data
    restart: unless-stopped
    networks:
      - rag_network

networks:
  rag_network:
    driver: bridge
